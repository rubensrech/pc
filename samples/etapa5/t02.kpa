/* >> If else, var init <<
 * Expect:  
 *      -> a (MEM(0)) = 100
 *      -> b (MEM(4)) = 10
 *      -> c (MEM(8)) = 55
 */

int main() {
    int a <= 15;
    int b <= 10;
    int c <= 0;
    if (a > b || c > 0) then {
        c = 55;
    } else {
        c = 66;
    }
    a = 100;
}