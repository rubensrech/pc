/* >> For <<
 * Expect:  
 *      -> a (MEM(0)) = 50
 *      -> b (MEM(4)) = 5
 *      -> c (MEM(8)) = -8
 *      -> i (MEM(12)) = 10
 */

int main() {
    int a <= 0;
    int b;
    int c;
    for (int i <= 0, c = 2 : i < 10 : i = i + 1, c = c - 1) {
        a = a + 5;
    }
    b = 5;
}