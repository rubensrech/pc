/* >> Break inside for <<
 * Expect: 
 *      -> a (MEM(0)) = 6
 *      -> i (MEM(4)) = 5
 */

int main() {
    int a <= 0;
    for (int i <= 0 : i < 10 : i = i + 1) {
        a = a + 1;
        if (i == 5) then {
            break;
        }
    }
}
